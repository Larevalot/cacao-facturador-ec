{
  "name": "cacaofacturador-ec",
  "version": "1.0.0",
  "description": "Facturador Electrónico SRI Ecuador e Inventario en Rust e Iced GUI",
  "type": "module",
  "scripts": {
    "dev": "cargo run",
    "desktop": "cargo run",
    "desktop:release": "cargo run --release",
    "serve": "cargo run -- --serve",
    "build": "cargo build --release",
    "test": "cargo test",
    "cli": "cargo run -- --cli"
  },
  "devDependencies": {
    "@sveltejs/vite-plugin-svelte": "^7.3.0",
    "@tauri-apps/cli": "^2.11.4",
    "svelte": "^5.56.8",
    "vite": "^8.2.1"
  },
  "packageManager": "pnpm@11.12.0",
  "engines": {
    "node": ">=22.0.0",
    "pnpm": ">=10.0.0"
  },
  "dependencies": {
    "@tauri-apps/api": "^2.11.1"
  }
}
